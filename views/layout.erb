<!DOCTYPE html>
<html lang="en">

  <%= erb :'furniture/header' %>

  <body>
    <div id="wrapper">
        <header>
            <h1><a id="logo" href="/">Prototypical</a></h1>
        </header>
        
        <div class="menu">
            <ul>
                <% Page.all.each do |p| %>
                    <li><a href="/page/<%= p.id %>"><%= p.title %></a></li>
                <% end %>
            </ul>
        </div>
                        
        <%= yield %>
    
    </div>
    
    <%= styled_flash %>
    <%= erb :'furniture/menu' %>
    <%= erb :'furniture/scripts' %>
    
  </body>
</html>